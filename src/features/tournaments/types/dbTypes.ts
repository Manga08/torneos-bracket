import type { Tournament, Participant, Match, Profile } from '@/types/database';

export type TournamentRow = Tournament;
export type ParticipantRow = Participant;
export type MatchRow = Match;
export type ProfileRow = Profile;
